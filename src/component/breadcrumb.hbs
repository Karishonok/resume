<div class="col-12">
    <div class="my-4 ms-3">
        <ol class="breadcrumb">
            {{#each breadcrumb}}
            <li class="breadcrumb-item">
                <a href="{{url}}">{{#if url}}{{name}}{{/if}}</a>
            </li>
            {{/each}}
        </ol>
    </div>
</div>